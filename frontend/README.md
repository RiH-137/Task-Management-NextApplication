# Frontend

Next.js app for the Team Task Manager.

## Setup

```bash
cp .env.example .env.local
npm install
npm run dev
```

Ensure `NEXT_PUBLIC_API_URL` points to the backend base URL.

## Key routes

- `/` dashboard and landing
- `/sign-in` Clerk sign-in
- `/sign-up` Clerk sign-up
